export default {
    errors: {

    },
    operation: {
        emptyErrs: null,
        formatErrors: errors => errors,
    },
    response: {
        emptyErrors: null,
        formatErrors: errors => errors,
        emptyData: null,
        formatData: data => data
    }
}
